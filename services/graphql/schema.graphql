type Query {
  user(email: String!): User
  users: [User]
}

type Mutation {
  registerUser(name: String!, userId: String!, email: String!, password: String!, userType: String!): User
}

type User {
  name: String!
  userId: String!
  email: String!
  password: String!
  userType: String!
}